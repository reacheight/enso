import type { FC } from '../../../lib/teact/teact';

import CommandPalette from './CommandPalette';

const CommandPaletteAsync: FC = () => {
  return <CommandPalette />;
};

export default CommandPaletteAsync;
